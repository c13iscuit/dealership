<h1>Cars Index</h1>

<%= link_to "Add a car!", new_car_path %>

<ul>
<% @cars.each do |car| %>
  <li><%= "#{car.year} #{car.manufacturer.name} - #{car.color}, #{car.mileage} miles" %>
  <p> Description: <%= car.description %></p></li>
<% end %>
